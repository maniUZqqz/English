# تاریخچه نسخه‌ها

## v1.0.0 — نسخه ۱ 🎉 (شهریور ۱۴۰۵ / اوت ۲۰۲۶)

اولین نسخه کامل «English Lady» — موسسه زبان هوشمند فارسی‌محور روی Django.

### پنج مهارت (هرکدام با سیستم آموزش کامل)
- **گرامر**: درس‌نامه سطح‌محور CEFR + آزمون خودکار + چک گرامر
- **لغت**: دیکشنری هوشمند + دفتر لغات + فلش‌کارت لایتنر ۵ جعبه‌ای
- **شنیداری**: تمرین شنیداری با پخش صوتی و سوال درک مطلب
- **نوشتاری**: تصحیح AI با نمره آیلتس، اصلاح خط‌به‌خط و نسخه بهبودیافته
- **اسپیکینگ**: مکالمه آزاد صوتی + تمرین تلفظ نمره‌دار کلمه‌به‌کلمه

### موسسه (کلاس آنلاین)
- نقش معلم/زبان‌آموز، کلاس با کد عضویت، برنامه هفتگی
- جلسات + حضورغیاب (حاضر/غایب/تأخیر/موجه)
- تکلیف با پیوست فایل، نمره ۰-۱۰۰ و بازخورد؛ مهلت تحویل اجباری
- آزمون کلاسی: تایمر سمت سرور، تصحیح خودکار، ساخت سوال دستی یا با AI
- کارنامه قابل چاپ (حضور + تکالیف + آزمون‌ها + میانگین کل)
- گفتگوی زیر تکلیف، اطلاعیه، **کلاس زنده** (Jitsi خودکار / لینک سفارشی)

### قابلیت‌های ویژه
- **معلم PDF**: آپلود کتاب، استخراج و بخش‌بندی متن، درس کامل هر بخش
  (خلاصه فارسی، جدول لغات، نکات گرامری، ترجمه)، افزودن لغات به دفتر، آزمون
- **چت‌بات با حافظه**: گفتگوها ذخیره و در جواب‌های بعدی لحاظ می‌شوند
- آزمون تعیین سطح با فیدبک فوری داخل صفحه (فقط ۲ فراخوانی AI)

### گیمیفیکیشن و درآمد
- XP برای هر فعالیت + سطح + لیدربرد با مدال
- استریک روزانه و نمودار فعالیت ۱۴ روزه
- اشتراک **زرین‌پال** (پایه/حرفه‌ای) با سقف AI پلن‌محور و حالت سندباکس

### زیرساخت
- رابط کامل فارسی RTL (وزیرمتن) + دارک‌مود
- PWA قابل نصب روی موبایل + Service Worker
- تنظیم کلید API و سقف مصرف از پنل ادمین (AIConfig)
- سهمیه روزانه AI، محدودیت تلاش ورود، بازیابی رمز با ایمیل
- تنظیمات امنیتی استقرار خودکار با `DEBUG=False`
- دیتابیس SQLite (کافی برای شروع؛ مهاجرت آسان در آینده)
- ۷۴ تست خودکار — بدون فراخوانی واقعی API

### پیش‌نیازهای فعال‌سازی کامل (سمت مدیر)
1. شارژ حساب MetisAI یا کلید API جدید → پنل ادمین ← AI Configuration
2. مرچنت‌کد زرین‌پال → `ZARINPAL_MERCHANT_ID` در `.env`
3. استقرار: هاست + دامنه + SSL + `DEBUG=False` + SMTP + سرو `media/`
