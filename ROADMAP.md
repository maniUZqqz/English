# نقشه راه English Lady 🎓

> سند محصول · ایده‌پردازی و نقشه راه — نسخه ۱.۰ — شهریور ۱۴۰۵ (اوت ۲۰۲۶)
>
> نسخه وب: https://claude.ai/code/artifact/ebff27b6-ae2d-41e1-919e-76250f0abd40

از یک پروژه دانشجویی دوتکه، به یک پلتفرم یکپارچه آموزش زبان انگلیسی با هوش مصنوعی — و مسیری که آن را به بازار می‌رساند.

---

## ۱ · محصول امروز کجاست

پس از یکپارچه‌سازی، همه‌چیز روی یک اپلیکیشن **Django** سوار است: آزمون تعیین سطح با تحلیل CEFR، درس‌نامه‌های گرامری شخصی‌سازی‌شده، آزمون‌ساز خودکار، و پنج ابزار هوش مصنوعی (چت‌بات، داستان‌ساز، مکالمه صوتی، چک گرامر، دیکشنری هوشمند) به‌علاوه یک قابلیت جدید: **دفتر لغات شخصی** که به داستان‌ساز وصل است — کاربر لغت ذخیره می‌کند و بعد با همان لغات داستان تمرین می‌کند.

| قبل | بعد |
|---|---|
| دو سرور جدا (Express + Django)، دو لاگین، دو طراحی | یک اپ جنگو، یک حساب کاربری، یک طراحی واحد |
| کلیدهای API داخل کد | کلیدها در `.env` خارج از کد و گیت |
| شروع آزمون یک نفر، آزمون بقیه را پاک می‌کرد | سوالات هر کاربر مستقل است |
| مکالمه صوتی فارسی می‌شنید و فارسی جواب می‌داد | تشخیص و تولید گفتار انگلیسی (en-US) |
| پرامپت system از سمت کاربر قابل دستکاری بود | پرامپت‌ها سمت سرور و سطح‌آگاه (level-aware) |

> ⚠️ **یک مانع فوری:** اعتبار حساب MetisAI تمام شده (خطای 402). تا حساب شارژ نشود یا کلید جدیدی در `.env` قرار نگیرد، بخش‌های هوش مصنوعی پاسخ نمی‌دهند. این اولین کار فاز ۱ است.

## ۲ · چشم‌انداز: چرا کسی English Lady را انتخاب کند؟

بازار پر از اپ آموزش زبان است؛ برگ برنده‌ی این محصول باید **«معلم خصوصیِ فارسی‌زبان»** بودن باشد: تعیین سطح می‌کند، درسنامه با ترجمه فارسی می‌سازد، اشتباهات همان کاربر را می‌شناسد و تمرین بعدی را بر اساس ضعف‌های او تولید می‌کند. اپ‌های جهانی (Duolingo و مشابه‌ها) محتوای ثابت دارند؛ اینجا هر درس و هر آزمون برای همان کاربر ساخته می‌شود.

## ۳ · ایده‌ها برای بهتر شدن

### یادگیری 📚
- **مرور لایتنر (Spaced Repetition)** روی دفتر لغات — لغت‌ها در فاصله‌های زمانی هوشمند مرور شوند؛ مهم‌ترین قابلیت ماندگاری.
- **مسیر یادگیری** — به‌جای موضوع آزادِ درس‌نامه، یک نقشه‌ی مرتب از مباحث بر اساس سطح CEFR که تیک می‌خورند.
- **ماژول Listening** — همان داستان‌ساز + پخش صوتی، با سوالات درک مطلب شنیداری.
- **ماژول Reading از PDF** — اپ نیمه‌کاره `PDF_reader` را زنده کنیم: کاربر متن/PDF بدهد، واژه‌های سخت و خلاصه و سوال بگیرد.

### تعامل و ماندگاری 🔥
- **استریک واقعی** — الان «روزهای عضویت» نمایش داده می‌شود نه روزهای فعالیت؛ ثبت فعالیت روزانه و جایزه‌ی زنجیره.
- **امتیاز و نشان** — XP برای هر درس/آزمون/مرور لغت؛ نشان‌ها برای نقاط عطف.
- **گزارش هفتگی AI** — هر هفته یک تحلیل کوتاه: چه پیشرفتی کردی، کجا ضعف داری، این هفته چه بخوانی.
- **یادآور ایمیلی/پیامکی** برای مرور لغات سررسیدشده.

### هوش مصنوعی 🤖
- **حافظه‌ی مکالمه** — تاریخچه چت در دیتابیس ذخیره شود تا معلم AI کاربر را «بشناسد».
- **استریم پاسخ‌ها (SSE)** — تایپ‌شدن زنده‌ی جواب؛ حس سرعت چند برابر می‌شود.
- **بانک خطاهای کاربر** — هر غلط گرامری ثبت شود و آزمون‌های بعدی روی همان‌ها تمرکز کنند.
- **ارزیابی خودکار کیفیت** — مجموعه تست برای خروجی‌های JSON مدل تا تغییر مدل/پرامپت چیزی را نشکند.

### زیرساخت 🏗️
- **PostgreSQL** به‌جای SQLite برای استقرار.
- **سهمیه مصرف** — سقف روزانه‌ی درخواست AI برای هر کاربر؛ بدون آن، هزینه API قابل کنترل نیست.
- **صف پس‌زمینه (Celery)** برای تولید درس‌نامه و سوال تا صفحه معطل API نماند.
- **Docker + CI** برای استقرار تکرارپذیر، و تست‌های خودکار.

## ۴ · نقشه راه تا بازار

### فاز ۰ — یکپارچه‌سازی ✅ انجام شد
- انتقال کامل ۵ ابزار Express به جنگو + حذف دوگانگی
- قالب واحد، لندینگ‌پیج، داشبورد آماری، دفتر لغات
- امن‌سازی کلیدها، رفع باگ چندکاربره، پرامپت‌های سطح‌آگاه

### فاز ۱ — پایدارسازی و استقرار (۲ تا ۴ هفته)
پیش‌نیاز هر حرکت بعدی:
- شارژ حساب MetisAI / کلید جدید — ✅ کلید API الان **از پنل ادمین** (AI Configuration) قابل تنظیم است؛ نیازی به دست زدن به فایل‌ها نیست
- ✅ سهمیه‌ی مصرف روزانه برای هر کاربر — پیاده‌سازی شد (`AI_DAILY_LIMIT`، پیش‌فرض ۱۰۰ در روز؛ شمارش اتمیک در `DailyActivity`)
- ✅ استریک واقعی فعالیت روزانه — پیاده‌سازی شد (از بخش «تعامل و ماندگاری»؛ نمایش در داشبورد)
- ✅ بازیابی رمز عبور — پیاده‌سازی شد (`/password-reset/`؛ در توسعه ایمیل در کنسول چاپ می‌شود، برای استقرار SMTP در `.env`)؛ تأیید ایمیل هنگام ثبت‌نام باقی مانده
- ✅ فارسی‌سازی کامل رابط (RTL + وزیرمتن) + دارک‌مود + صفحه پروفایل + نمودار پیشرفت
- استقرار: هاست ایرانی + PostgreSQL + دامنه + SSL + `DEBUG=False`
- ✅ تست‌های خودکار برای مسیرهای اصلی — ۲۲ تست (احراز هویت، تعیین سطح per-user، سهمیه، استریک، ابزارها، دفتر لغات)

### فاز ۲ — درآمدزایی (۴ تا ۸ هفته)
لندینگ الان پلن‌ها را نمایش می‌دهد؛ باید واقعی شوند:
- ✅ درگاه پرداخت زرین‌پال + مدل اشتراک — پیاده‌سازی شد (`billing/`؛ پلن پایه/حرفه‌ای، سندباکس، تمدید از انقضای فعلی؛ فقط `ZARINPAL_MERCHANT_ID` لازم است)
- ✅ پلن رایگان محدود → پولی — سقف AI پلن‌محور شد (اشتراک > ادمین > .env)
- پنل ادمین: کاربران، مصرف، درآمد (مدل‌ها در ادمین ثبت‌اند؛ داشبورد مدیریتی باقی مانده)
- آنالیتیکس رفتار کاربر (کدام ابزار استفاده می‌شود، کجا رها می‌کنند)

### فاز ۳ — تمایز محصول (۲ تا ۳ ماه)
قابلیت‌هایی که رقبا ندارند:
- ✅ مرور لایتنر روی دفتر لغات — پیاده‌سازی شد (فلش‌کارت ۵ جعبه‌ای، `/tools/review/`)؛ یادآور ایمیلی باقی مانده
- مسیر یادگیری CEFR و بانک خطاهای شخصی
- ✅ پنج مهارت کامل — نوشتاری (نمره آیلتس + اصلاح خط‌به‌خط) و تلفظ نمره‌دار اضافه شد؛ صفحه `/skills/`
- ✅ معلم PDF (`library/`) — آپلود کتاب، درس هر بخش، لغات به دفتر، آزمون درک مطلب
- ✅ گیمیفیکیشن XP + لیدربرد؛ ✅ کلاس زنده (Jitsi خودکار / لینک سفارشی)؛ ✅ PWA
- ✅ حافظه مکالمه چت‌بات — پیاده‌سازی شد (ذخیره در DB + دکمه گفتگوی جدید)؛ استریم پاسخ‌ها باقی مانده
- ✅ تمرین شنیداری (Listening) — پیاده‌سازی شد (`/tools/listening/`)
- PWA برای نصب روی موبایل

### فاز ۴ — رشد (پس از تثبیت)
وقتی retention قابل قبول شد:
- سیستم دعوت دوستان (اعتبار رایگان برای هر دو طرف)
- بلاگ و سئوی فارسی («آموزش گرامر»، «تعیین سطح آنلاین»)
- ✅ فروش B2B به آموزشگاه‌ها و مدارس — هسته‌اش پیاده‌سازی شد: ماژول «کلاس زبان» (`classroom`) با نقش معلم/زبان‌آموز، کلاس با کد عضویت، تکلیف و تحویل، نمره‌دهی و فیدبک، اطلاعیه، و گزارش پیشرفت زبان‌آموزها برای معلم

## ۵ · ریسک‌ها

| ریسک | راهکار |
|---|---|
| هزینه‌ی API با رشد کاربران منفجر می‌شود | سهمیه مصرف، کش کردن درس‌نامه‌ها (الان هست)، مدل ارزان‌تر برای کارهای ساده |
| وابستگی به یک ارائه‌دهنده (MetisAI) | لایه `services.py` ساخته شد؛ تعویض provider فقط تغییر دو متغیر `.env` است |
| کیفیت ناپایدار خروجی مدل (JSON خراب و…) | پارسر مقاوم نوشته شد؛ قدم بعد: مجموعه ارزیابی خودکار پرامپت‌ها |
| Web Speech API روی همه مرورگرها نیست | پیام جایگزین نمایش داده می‌شود؛ در آینده: STT سمت سرور (Whisper) |

## ۶ · متریک‌های موفقیت

- **فعال‌سازی:** درصد ثبت‌نامی‌هایی که آزمون تعیین سطح را کامل می‌کنند
- **ماندگاری:** بازگشت در روز ۷ (D7 Retention)
- **عمق استفاده:** تعداد درس‌نامه و لغت ذخیره‌شده به‌ازای هر کاربر فعال
- **درآمد:** نرخ تبدیل رایگان ← پولی، و هزینه AI به‌ازای هر کاربر فعال
