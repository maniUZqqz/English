# راهنمای راه‌اندازی و تست — English Lady 🧪

این فایل ۴ کاری که برای رسیدن به لانچ باید خودت انجام بدهی را قدم‌به‌قدم توضیح می‌دهد.

> ⚡ برای اجرای سریع پروژه فقط روی **`run.bat`** در ریشه پروژه دابل‌کلیک کن.

---

## ۱ · کلید API هوش مصنوعی 🔑

بدون این، همه بخش‌های AI (تعیین سطح، درس‌نامه، ابزارها، معلم PDF) خطای «سرویس در دسترس نیست» می‌دهند.

### گزینه الف — MetisAI (همان سرویس فعلی، بدون تحریم)
1. برو به **metisai.ir** و وارد پنل شو (حساب قبلی داری — اعتبارش تمام شده، خطای 402)
2. حساب را **شارژ** کن (بخش کیف پول / پرداخت)
3. اگر کلید جدید خواستی: بخش API Keys ← ساخت کلید جدید (با `tpsg-` شروع می‌شود)
4. کلید را وارد کن — **بدون دست‌زدن به کد**:
   - `python manage.py runserver` ← برو به `http://127.0.0.1:8000/admin/`
   - بخش **AI Configuration** ← فیلد `api_key` ← ذخیره
5. ⚠️ چون فایل `.env` روی GitHub رفته، کلید قبلی را «لو رفته» فرض کن و همیشه کلید جدید را فقط در **پنل ادمین** بگذار (ادمین بر `.env` اولویت دارد).

### گزینه ب — هر سرویس سازگار با OpenAI
AvalAI، Gapgpt، یا خود OpenAI (با تحریم‌شکن). فقط در پنل ادمین این دو را عوض کن:
- `base_url` ← آدرس سرویس (مثلاً `https://api.avalai.ir/v1`)
- `api_key` ← کلید همان سرویس

### تست اینکه وصل شد
وارد سایت شو ← **چک گرامر** (`/tools/grammar/`) ← بنویس `I goes to school` ← اگر جواب اصلاح‌شده آمد، وصل است ✅

---

## ۲ · تست انسانی قابلیت‌ها ✋

بعد از فعال‌شدن AI، این چک‌لیست را به ترتیب برو. برای بخش کلاس **دو مرورگر** لازم داری (یکی معلم، یکی زبان‌آموز — یا یک پنجره معمولی + یک Incognito).

### الف) مسیر زبان‌آموز جدید (مرورگر ۱)
- [ ] ثبت‌نام کاربر جدید ← باید مستقیم وارد داشبورد شوی
- [ ] **تعیین سطح**: ۱۰ سوال، بعد از هر جواب فیدبک سبز/قرمز فوری داخل صفحه، آخرش تحلیل سطح CEFR
- [ ] **درس‌نامه** (`/qqz/Teach/`): دکمه «پیشنهاد موضوع با AI» و ساخت درس ← دفعه دوم همان موضوع باید فوری بیاید (کش)
- [ ] **آزمون از درس‌نامه**: جواب بده — گزینه درست فقط *بعد از* جواب‌دادن سبز شود
- [ ] **۵ مهارت** (`/skills/`): هر ۶ کارت باز شوند
- [ ] **نوشتاری**: «سوال آیلتس» بگیر ← یک متن ۵۰+ کلمه‌ای بنویس ← نمره + باند + اصلاحات + نسخه بهبود
- [ ] **تلفظ**: جمله‌ها را بساز ← بشنو ← بخوان (فقط کروم!) ← کلمه‌ها سبز/قرمز شوند
- [ ] **شنیداری**: تمرین بساز ← فقط صدا پخش شود ← بعد از «بررسی پاسخ‌ها» متن نمایان شود
- [ ] **دیکشنری**: یک کلمه تکی ترجمه کن ← «ذخیره در دفتر لغات» ← در `/tools/wordbank/` ببینش
- [ ] **مرور لایتنر** (`/tools/review/`): کارت را ورق بزن، «بلد بودم» بزن ← جعبه ۲ شود
- [ ] **چت‌بات**: دو پیام بفرست ← صفحه را رفرش کن ← تاریخچه بماند ← «گفتگوی جدید» پاکش کند
- [ ] **معلم PDF** (`/library/`): یک PDF متنی انگلیسی آپلود کن ← بخش‌ها ساخته شوند ← «این بخش را درس بده» ← «استخراج لغات» ← آزمون
- [ ] **داشبورد**: استریک، XP، نمودار فعالیت و سهمیه مصرف تغییر کرده باشند
- [ ] **لیدربرد** (`/leaderboard/`) و **پروفایل** (تغییر رمز) و **دارک‌مود** 🌙

### ب) مسیر معلم (مرورگر ۲)
- [ ] در ادمین ← User profiles ← نقش یک کاربر را Teacher کن
- [ ] کلاس بساز ← کد عضویت را بردار
- [ ] در مرورگر ۱ (زبان‌آموز) با کد عضو شو
- [ ] برنامه هفتگی اضافه کن ← روی داشبورد زبان‌آموز «جلسه بعدی» بیاید
- [ ] جلسه ثبت کن ← حضورغیاب بزن
- [ ] تکلیف با مهلت بساز ← زبان‌آموز جواب + فایل بفرستد ← نمره و بازخورد بده ← زبان‌آموز ببیند
- [ ] یک تکلیف با مهلتِ گذشته بساز ← زبان‌آموز **نتواند** تحویل دهد
- [ ] آزمون کلاسی بساز (سوال دستی + «تولید ۵ سوال با AI») ← منتشر کن ← زبان‌آموز شرکت کند ← وسط آزمون **رفرش کند و تایمر ادامه یابد** ← نمره خودکار
- [ ] کارنامه زبان‌آموز را باز کن + دکمه چاپ
- [ ] «کلاس زنده» ← اتاق Jitsi باز شود (هر دو مرورگر واردش شوید)

### ج) موبایل
- [ ] با گوشی (همان وای‌فای) به `http://IP-سیستم:8000` برو — منو، چت‌ها و نصب PWA (افزودن به صفحه اصلی) را چک کن
  - برای این تست سرور را این‌طور اجرا کن: `python manage.py runserver 0.0.0.0:8000`

هر جا مشکلی دیدی، متن خطا/اسکرین‌شات را نگه دار و بیار تا رفع کنم.

---

## ۳ · مرچنت‌کد زرین‌پال 💳

1. برو به **zarinpal.com** ← ثبت‌نام (کد ملی + شبا لازم است)
2. بعد از تأیید، یک **درگاه** بساز ← «مرچنت‌کد» ۳۶ کاراکتری را کپی کن
3. در فایل `python/EnglishWoman/.env`:
   ```
   ZARINPAL_MERCHANT_ID=کد-۳۶-کاراکتری
   ZARINPAL_SANDBOX=True
   SITE_URL=http://127.0.0.1:8000
   ```
4. **تست سندباکس** (پول واقعی رد و بدل نمی‌شود): سرور را ری‌استارت کن ← `/billing/plans/` ← «پرداخت با زرین‌پال» ← در صفحه سندباکس، پرداخت موفق را شبیه‌سازی کن ← باید برگردی و پیام «پرداخت موفق 🎉» + اشتراک فعال ببینی ← سقف AI روی داشبورد بالا رفته باشد
5. برای پول واقعی: `ZARINPAL_SANDBOX=False` و `SITE_URL` را آدرس واقعی سایت بگذار (درگاه واقعی فقط روی دامنه تأییدشده در پنل زرین‌پال کار می‌کند)

---

## ۴ · خرید هاست و بالا بردن سایت 🌐

دیتابیس SQLite است، پس ساده‌ترین مسیرها:

### گزینه الف — لیارا (liara.ir) — پیشنهاد برای شروع (PaaS، بدون سرورداری)
1. ثبت‌نام در **liara.ir** ← ساخت برنامه از نوع **Django**
2. `liara deploy` از ریشه `python/EnglishWoman` (طبق داکیومنت لیارا) یا اتصال به همین مخزن GitHub
3. متغیرهای محیطی را در پنل لیارا بگذار:
   ```
   DJANGO_DEBUG=False
   DJANGO_SECRET_KEY=یک-رشته-تصادفی-جدید-و-طولانی
   DJANGO_ALLOWED_HOSTS=دامنه-ات.ir
   SITE_URL=https://دامنه-ات.ir
   ZARINPAL_MERCHANT_ID=...
   ZARINPAL_SANDBOX=False
   ```
4. یک **دیسک** برای `media/` و `db.sqlite3` بساز (تا با هر دیپلوی پاک نشوند)
5. دامنه + SSL رایگان را از پنل لیارا وصل کن

### گزینه ب — سرور مجازی (پارس‌پک، ایران‌سرور، آروان)
1. VPS اوبونتو بگیر ← `git clone https://github.com/maniUZqqz/English.git`
2. `pip install -r python/requirements.txt gunicorn`
3. `.env` را مثل بالا تنظیم کن ← `python manage.py migrate` ← `python manage.py collectstatic`
4. اجرا با gunicorn: `gunicorn EnglishWoman.wsgi -b 127.0.0.1:8001`
5. nginx جلوی آن: مسیر `/static/` به پوشه `staticfiles/` و `/media/` به پوشه `media/` ← دامنه + SSL با certbot

### بعد از بالا آمدن
- `https://دامنه/admin/` باز شود و کلید AI را در AI Configuration بگذار
- یک ثبت‌نام + یک درس + یک پرداخت واقعی کوچک تست کن
- ایمیل: برای بازیابی رمز واقعی، `EMAIL_*` را در env با SMTP (مثلاً Gmail App Password یا سرویس ایرانی) تنظیم کن

---

## ترتیب پیشنهادی
۱ (کلید AI) ← ۲ (تست کامل لوکال) ← ۳ (زرین‌پال سندباکس) ← ۴ (هاست) ← تست نهایی روی دامنه ← بتای بسته با چند دوست ← لانچ 🚀
